[project]
name = "webhook-subscriber-example"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "faster-whisper",
    "requests",
]

[dependency-groups]
dev = [
    "ruff",
]

[tool.ruff]
line-length = 100
