[project]
name = "segment-batch-fetcher"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "requests",
    "beautifulsoup4",
    "tqdm",
]

[dependency-groups]
dev = [
    "pytest",
    "requests-mock",
    "ruff",
]

[tool.ruff]
line-length = 100
